@model List<Product>
<section class="section" id="social">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="section-heading">
                    <h2>Son Eklenen Ürünler</h2>
                    
                </div>
            </div>
        </div>
    </div>
    <div class="container">
        <div class="row images">
            @foreach (var item in Model)
            {
                <div class="col-2">
                    <div class="thumb">
                        <a href="#"> <img src="@item.ProductImageCover" alt=""></a>
                       
                    </div>
                </div>
            }

        </div>
    </div>
</section>
